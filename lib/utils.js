const utils = {};

utils.fileExtension = (URL) => {
    return URL.split('.')[1];
}

export { utils };


//service tai undefines
// services/design tai underfined
//mes nuoroda splitinam ir jei tarkim bus css/main.css tai grazins css